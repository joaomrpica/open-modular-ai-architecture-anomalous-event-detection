PLUGIN_TYPE = "audio"
# Path to a VOSK model directory (relative to plugin dir or absolute). Default
# expects a small Portuguese model in the plugin folder named `vosk-model-small-pt`.
MODEL_PATH = "vosk-model-small-pt"
# Forced language tag (informational)
LANGUAGE = "pt"
